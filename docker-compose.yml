data:
  build: . 
  volumes:
    - /home/wordpress
  command: true
app:
  build: . 
  links:
    - mysql
    - redis
    - memcached
  env_file: .env
  volumes_from:
    - data
  volumes:
    - /var/ssl:/home/wordpress/conf/certs
proxy:
  image: jwilder/nginx-proxy
  ports:
    - "80:80"
    - "443:443"
  volumes:
    - /var/ssl:/etc/nginx/certs
    - /var/run/docker.sock:/tmp/docker.sock:ro
mysql:
  image: mariadb
  environment:
    - MYSQL_ROOT_PASSWORD=mysecretpassword
redis:
  image: redis
memcached:
  image: memcached


